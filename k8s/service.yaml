apiVersion: v1
kind: Service
metadata:
  name: constructai-service
spec:
  selector:
    app: constructai
  type: NodePort
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30001